# AXIS

This project was generated with Angular CLI version 18.2.19.

## Development server
Run `ng serve` for a dev server. Navigate to `http://localhost:4200/`.

## Build
Run `ng build` to build the project.

## Running unit tests
Run `ng test`

## Further help
Use `ng help` for more info.